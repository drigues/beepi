<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Beepicc</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
  </head>

  <body class="bg-[#1e2f10] text-white font-sans">
    <!-- Navigation -->
    <nav class="flex items-center justify-between px-6 py-4 shadow-md border-b border-[#2a4712]">
      <div class="flex items-center space-x-2">
        <%= inline_svg("beepicc.svg", class: "w-8 h-8 text-[#d1ff00]") %>
        <a href="<%= root_path %>" class="text-2xl font-bold text-[#d1ff00]">Beepicc</a>
      </div>
      <div class="space-x-4 text-sm">
        <% if logged_in? %>
          <%= link_to "Dashboard", dashboard_path, class: "hover:text-[#d1ff00] transition" %>
          <span>Hi, <%= current_user.name.presence || current_user.email %></span>
          <%= button_to "Logout", logout_path, method: :delete, class: "inline-form hover:text-red-400 transition" %>
        <% else %>
          <%= link_to "Login", login_path, class: "hover:text-[#d1ff00] transition" %>
          <%= link_to "Sign Up", signup_path, class: "hover:text-[#d1ff00] transition" %>
        <% end %>
      </div>
    </nav>

    <!-- Page Content -->
    <main class="flex-grow">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-[#0d1a0b] py-8 text-center border-t border-[#2a4712] text-sm">
      <div class="space-x-6">
        <%= link_to "Terms", terms_path, class: "hover:text-[#d1ff00] transition" %>
        <%= link_to "Policy", policy_path, class: "hover:text-[#d1ff00] transition" %>
        <%= link_to "Cookies", cookies_path, class: "hover:text-[#d1ff00] transition" %>
      </div>
    </footer>
  </body>
</html>
