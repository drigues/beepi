<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Beepicc</title>
    <%= favicon_link_tag 'favicon.png' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
  </head>

  <body class="bg-white text-black font-sans">
    <!-- Header -->
    <header class="fixed top-0 left-0 right-0 z-50 bg-white shadow-md">
      <div class="max-w-7xl mx-auto flex items-center justify-between px-6 py-4">
        <a href="<%= root_path %>" class="flex items-center space-x-2 text-xl font-bold">
          <%= inline_svg("beepicc.svg", class: "w-2xs h-10") %>
        </a>
        <div class="space-x-4 text-sm">
          <% if logged_in? %>
            <%= link_to "Dashboard", dashboard_path, class: "bg-black text-white px-4 py-2 rounded-full hover:bg-gray-800 transition" %>
            <%= button_to "Logout", logout_path, method: :delete, class: "border border-black rounded-full px-4 py-2 hover:bg-black hover:text-white transition" %>
            <span>Hi, <%= current_user.name.presence || current_user.email %></span>    
          <% else %>
            <%= link_to "Login", login_path, class: "border border-black rounded-full px-4 py-2 hover:bg-black hover:text-white transition" %>
            <%= link_to "Sign Up Free", signup_path, class: "bg-black text-white px-4 py-2 rounded-full hover:bg-gray-800 transition" %>
          <% end %>
        </div>
      </div>
    </header>

    <!-- Page Content -->
    <main>
       <!-- Spacer for fixed header -->
      <div class="h-16"></div>

      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-black text-white py-10">
      <div class="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-4 gap-6 text-sm">
        <div>
          <!-- Footer <%= image_tag 'beepicc.svg', class: "w-24 h-auto mb-4" %> -->
          <p class="opacity-60">hello@beepi.cc</p>
        </div>
        <div>
          <h4 class="font-bold mb-2">Home</h4>
          <ul class="space-y-1">
            <li><%= link_to "How it works", "#", class: "hover:text-pink-400" %></li>
            <li><%= link_to "Cases", "#", class: "hover:text-pink-400" %></li>
            <li><%= link_to "FAQ's", "#", class: "hover:text-pink-400" %></li>
          </ul>
        </div>
        <div>
          <h4 class="font-bold mb-2">Pages</h4>
          <ul class="space-y-1">
            <li><%= link_to "Home", root_path, class: "hover:text-pink-400" %></li>
            <li><%= link_to "Cookies", cookies_path, class: "hover:text-pink-400" %></li>
            <li><%= link_to "Privacy", policy_path, class: "hover:text-pink-400" %></li>
            <li><%= link_to "Terms", terms_path, class: "hover:text-pink-400" %></li>
            <li><%= link_to "Login", login_path, class: "hover:text-pink-400" %></li>
          </ul>
        </div>
        <div>
          <h4 class="font-bold mb-2">Social</h4>
          <ul class="space-y-1">
            <li><a href="#" class="hover:text-pink-400">Linkedin</a></li>
            <li><a href="#" class="hover:text-pink-400">Instagram</a></li>
          </ul>
        </div>
      </div>
      <div class="text-center text-xs text-gray-400 mt-8">
        &copy; <%= Time.current.year %> Beepicc. All rights reserved.
      </div>
    </footer>
  </body>
</html>
