<!DOCTYPE html>
<html>
  <head>
    <title><%= @user&.name || "Profile" %> | AuthDemo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <style>
      body { background: #f8fafc; font-family: 'Segoe UI', Arial, sans-serif; margin: 0; padding: 0; }
      .profile-container { max-width: 340px; margin: 7em auto 0 auto; background: #fff; border-radius: 10px; box-shadow: 0 2px 16px rgba(0,0,0,0.11); padding: 2.5em 2em; text-align: center; }
      .profile-name { font-size: 2.1em; font-weight: 700; color: #111; margin-bottom: 0.3em; }
      .profile-bio { font-size: 1.15em; color: #444; margin-top: 1em; white-space: pre-line; }
      .profile-directory { font-size: 0.95em; color: #888; margin-top: 2em; }
      .profile-404-container { max-width: 400px; margin: 7em auto 0 auto; background: #fff; border-radius: 10px; box-shadow: 0 2px 16px rgba(0,0,0,0.11); padding: 2.5em 2em; text-align: center; }
      .claim-form input[type="text"] { font-size: 1.1em; padding: 0.5em; margin-bottom: 1em; width: 70%; border-radius: 5px; border: 1px solid #bbb; }
      .btn { display: inline-block; padding: 0.5em 1.3em; background: #2ea44f; color: #fff; border: none; border-radius: 4px; font-size: 1em; cursor: pointer; text-decoration: none; }
      .btn-primary { background: #2ea44f; }
    </style>
  </head>
  <body>
    <div class="profile-container">
      <%= yield %>
    </div>
  </body>
</html>