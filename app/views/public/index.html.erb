<!-- Hero Section -->
<section class="text-center py-28 px-6 bg-gradient-to-br from-pink-200 via-white to-purple-200">
  <h1 class="text-4xl md:text-6xl font-extrabold text-black">
    A Link in Bio made for you!
  </h1>
  <p class="mt-4 text-base text-black">
    <span class="text-pink-600 font-semibold">200K+</span> Be one of the early-birds and claim your <em class="italic text-purple-600">beepi.cc/URL</em> before the anyone else
  </p>

  <!-- Claim Form -->
  <div class="flex flex-col sm:flex-row justify-center items-center gap-4 h-24">
    <input type="text" placeholder="beepi.cc/yourname" class="px-6 py-3 w-64 sm:w-80 text-black rounded-full border border-gray-300 shadow" />
    <%= form_with url: signup_path, method: :get, local: true do %>
      <%= submit_tag "Claim Now", class: "bg-white text-black border border-black hover:bg-black hover:text-white px-6 py-3 rounded-full transition" %>
    <% end %>
  </div>
</section>

<!-- Feature Section 1 -->
<section class="bg-pink-200">
  <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-10 items-center py-20 px-6">
      <div class="space-y-4 text-left px-4">
        <h2 class="text-3xl font-bold text-black">All your tasks organized</h2>
        <p class="text-gray-800">Effortlessly manage tasks, collaborate with teams, and meet deadlines with precision and clarity.</p>
        <%= link_to "Get Start Free", signup_path, class: "bg-black text-white px-6 py-3 rounded-full inline-block hover:bg-gray-800 transition" %>
      </div>
      <div>
        <%= image_tag "image1.png", class: "w-full rounded-xl shadow-lg" %>
      </div>
  </div>

</section>

<!-- Feature Section 2 -->
<section class="py-20 px-6 text-center bg-purple-200">
  <div class="max-w-4xl mx-auto">
    <%= image_tag "image2.png", class: "mx-auto rounded-xl shadow-xl mb-8" %>
    <h2 class="text-3xl font-bold text-black">Everything you are. In one, simple link in bio.</h2>
    <p class="mt-4 text-gray-700 max-w-2xl mx-auto">
      Organize your identity, promote your business, and stay connected with your community—all in one place.
    </p>
  </div>
</section>

<!-- Final CTA -->
<section class="text-center py-20 px-6 bg-peach">
  <h2 class="text-3xl font-extrabold text-black">Claim your Link in Bio <span class="text-transparent bg-clip-text bg-gradient-to-r from-pink-500 via-orange-500 to-purple-500">NOW!</span></h2>
  <p class="mt-4 text-gray-700">Start your free trial now to experience seamless project management without any commitment!</p>

  <!-- Claim Form -->
  <div class="mt-8 flex flex-col sm:flex-row justify-center items-center gap-4">
    <input type="text" placeholder="beepi.cc/yourname" class="px-6 py-3 w-64 sm:w-80 text-black rounded-full border border-gray-300 shadow" />
    <%= form_with url: signup_path, method: :get, local: true do %>
      <%= submit_tag "CLAIM", class: "bg-white text-black border border-black hover:bg-black hover:text-white px-6 py-3 rounded-full transition" %>
    <% end %>
  </div>
</section>
